<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicustomer.mapper.TaskReminderMapper">

    <resultMap id="BaseResultMap" type="com.aicustomer.entity.TaskReminder">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="reminder_type" property="reminderType" jdbcType="INTEGER"/>
        <result column="customer_id" property="customerId" jdbcType="BIGINT"/>
        <result column="customer_name" property="customerName" jdbcType="VARCHAR"/>
        <result column="reminder_time" property="reminderTime" jdbcType="TIMESTAMP"/>
        <result column="deadline" property="deadline" jdbcType="TIMESTAMP"/>
        <result column="priority" property="priority" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="assigned_user_id" property="assignedUserId" jdbcType="BIGINT"/>
        <result column="assigned_user_name" property="assignedUserName" jdbcType="VARCHAR"/>
        <result column="reminder_method" property="reminderMethod" jdbcType="INTEGER"/>
        <result column="frequency" property="frequency" jdbcType="INTEGER"/>
        <result column="repeat_count" property="repeatCount" jdbcType="INTEGER"/>
        <result column="repeated_count" property="repeatedCount" jdbcType="INTEGER"/>
        <result column="next_reminder_time" property="nextReminderTime" jdbcType="TIMESTAMP"/>
        <result column="completed_time" property="completedTime" jdbcType="TIMESTAMP"/>
        <result column="completion_result" property="completionResult" jdbcType="VARCHAR"/>
        <result column="customer_response_time" property="customerResponseTime" jdbcType="TIMESTAMP"/>
        <result column="customer_response" property="customerResponse" jdbcType="VARCHAR"/>
        <result column="effectiveness_score" property="effectivenessScore" jdbcType="INTEGER"/>
        <result column="business_id" property="businessId" jdbcType="BIGINT"/>
        <result column="business_type" property="businessType" jdbcType="VARCHAR"/>
        <result column="rule_id" property="ruleId" jdbcType="BIGINT"/>
        <result column="is_auto_generated" property="isAutoGenerated" jdbcType="BOOLEAN"/>
        <result column="generated_from" property="generatedFrom" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="VARCHAR"/>
        <result column="update_by" property="updateBy" jdbcType="VARCHAR"/>
        <result column="deleted" property="deleted" jdbcType="TINYINT"/>
        <result column="version" property="version" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, content, reminder_type, customer_id, customer_name, reminder_time, deadline,
        priority, status, assigned_user_id, assigned_user_name, reminder_method, frequency,
        repeat_count, repeated_count, next_reminder_time, completed_time, completion_result,
        customer_response_time, customer_response, effectiveness_score, business_id, business_type,
        rule_id, is_auto_generated, generated_from, create_time, update_time, create_by, update_by,
        deleted, version
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM task_reminder
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM task_reminder
        WHERE deleted = 0
        <if test="reminder.reminderType != null">
            AND reminder_type = #{reminder.reminderType}
        </if>
        <if test="reminder.priority != null">
            AND priority = #{reminder.priority}
        </if>
        <if test="reminder.status != null">
            AND status = #{reminder.status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM task_reminder
        WHERE deleted = 0
        <if test="reminder.reminderType != null">
            AND reminder_type = #{reminder.reminderType}
        </if>
        <if test="reminder.priority != null">
            AND priority = #{reminder.priority}
        </if>
        <if test="reminder.status != null">
            AND status = #{reminder.status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM task_reminder
        WHERE deleted = 0
        <if test="reminder.reminderType != null">
            AND reminder_type = #{reminder.reminderType}
        </if>
        <if test="reminder.priority != null">
            AND priority = #{reminder.priority}
        </if>
        <if test="reminder.status != null">
            AND status = #{reminder.status}
        </if>
    </select>

    <insert id="insert" parameterType="com.aicustomer.entity.TaskReminder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task_reminder (
            title, content, reminder_type, customer_id, customer_name, reminder_time, deadline,
            priority, status, assigned_user_id, assigned_user_name, reminder_method, frequency,
            repeat_count, repeated_count, next_reminder_time, completed_time, completion_result,
            customer_response_time, customer_response, effectiveness_score, business_id, business_type,
            rule_id, is_auto_generated, generated_from, create_time, update_time, create_by, update_by,
            deleted, version
        ) VALUES (
            #{title}, #{content}, #{reminderType}, #{customerId}, #{customerName}, #{reminderTime}, #{deadline},
            #{priority}, #{status}, #{assignedUserId}, #{assignedUserName}, #{reminderMethod}, #{frequency},
            #{repeatCount}, #{repeatedCount}, #{nextReminderTime}, #{completedTime}, #{completionResult},
            #{customerResponseTime}, #{customerResponse}, #{effectivenessScore}, #{businessId}, #{businessType},
            #{ruleId}, #{isAutoGenerated}, #{generatedFrom}, #{createTime}, #{updateTime}, #{createBy}, #{updateBy},
            #{deleted}, #{version}
        )
    </insert>

    <update id="updateById" parameterType="com.aicustomer.entity.TaskReminder">
        UPDATE task_reminder
        SET title = #{title},
            content = #{content},
            reminder_type = #{reminderType},
            customer_id = #{customerId},
            customer_name = #{customerName},
            reminder_time = #{reminderTime},
            deadline = #{deadline},
            priority = #{priority},
            status = #{status},
            assigned_user_id = #{assignedUserId},
            assigned_user_name = #{assignedUserName},
            reminder_method = #{reminderMethod},
            frequency = #{frequency},
            repeat_count = #{repeatCount},
            repeated_count = #{repeatedCount},
            next_reminder_time = #{nextReminderTime},
            completed_time = #{completedTime},
            completion_result = #{completionResult},
            customer_response_time = #{customerResponseTime},
            customer_response = #{customerResponse},
            effectiveness_score = #{effectivenessScore},
            business_id = #{businessId},
            business_type = #{businessType},
            rule_id = #{ruleId},
            is_auto_generated = #{isAutoGenerated},
            generated_from = #{generatedFrom},
            update_time = #{updateTime},
            update_by = #{updateBy},
            version = version + 1
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE task_reminder
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectTodayReminders" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM task_reminder
        WHERE deleted = 0 AND DATE(deadline) = CURDATE()
        ORDER BY deadline ASC
    </select>

    <select id="selectUpcomingReminders" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM task_reminder
        WHERE deleted = 0 AND deadline BETWEEN NOW() AND DATE_ADD(NOW(), INTERVAL #{days} DAY)
        ORDER BY deadline ASC
    </select>

    <select id="selectByCustomerId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM task_reminder
        WHERE customer_id = #{customerId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <update id="completeTask">
        UPDATE task_reminder
        SET status = 3, completed_time = NOW(), update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="postponeTask">
        UPDATE task_reminder
        SET deadline = #{newDueDate}, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>




