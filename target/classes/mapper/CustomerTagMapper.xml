<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicustomer.mapper.CustomerTagMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.aicustomer.entity.CustomerTag">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="customer_id" property="customerId" jdbcType="BIGINT"/>
        <result column="tag_name" property="tagName" jdbcType="VARCHAR"/>
        <result column="tag_type" property="tagType" jdbcType="INTEGER"/>
        <result column="tag_color" property="tagColor" jdbcType="VARCHAR"/>
        <result column="tag_description" property="tagDescription" jdbcType="VARCHAR"/>
        <result column="weight" property="weight" jdbcType="INTEGER"/>
        <result column="is_visible" property="isVisible" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="VARCHAR"/>
        <result column="update_by" property="updateBy" jdbcType="VARCHAR"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
        <result column="version" property="version" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, customer_id, tag_name, tag_type, tag_color, tag_description,
        weight, is_visible, create_time, update_time, create_by, update_by,
        deleted, version
    </sql>

    <!-- 根据客户ID查询标签列表 -->
    <select id="selectByCustomerId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM customer_tag
        WHERE customer_id = #{customerId} AND deleted = 0
        ORDER BY weight DESC, create_time DESC
    </select>

    <!-- 插入客户标签 -->
    <insert id="insert" parameterType="com.aicustomer.entity.CustomerTag" useGeneratedKeys="true" keyProperty="tag.id">
        INSERT INTO customer_tag (
            customer_id, tag_name, tag_type, tag_color, tag_description,
            weight, is_visible, create_time, update_time, create_by, update_by,
            deleted, version
        ) VALUES (
            #{tag.customerId}, #{tag.tagName}, #{tag.tagType}, #{tag.tagColor},
            #{tag.tagDescription}, #{tag.weight}, #{tag.isVisible},
            #{tag.createTime}, #{tag.updateTime}, #{tag.createBy}, #{tag.updateBy},
            #{tag.deleted}, #{tag.version}
        )
    </insert>

    <!-- 根据ID删除客户标签（逻辑删除） -->
    <update id="deleteById" parameterType="java.lang.Long">
        UPDATE customer_tag
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 根据客户ID删除所有标签（逻辑删除） -->
    <update id="deleteByCustomerId" parameterType="java.lang.Long">
        UPDATE customer_tag
        SET deleted = 1, update_time = NOW()
        WHERE customer_id = #{customerId} AND deleted = 0
    </update>

</mapper>

