<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicustomer.mapper.KnowledgeDocumentMapper">

    <resultMap id="BaseResultMap" type="com.aicustomer.entity.KnowledgeDocument">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="file_name" property="fileName" />
        <result column="file_type" property="fileType" />
        <result column="file_size" property="fileSize" />
        <result column="file_path" property="filePath" />
        <result column="document_type" property="documentType" />
        <result column="tags" property="tags" />
        <result column="category" property="category" />
        <result column="summary" property="summary" />
        <result column="keywords" property="keywords" />
        <result column="view_count" property="viewCount" />
        <result column="download_count" property="downloadCount" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <sql id="Base_Column_List">
        id, title, content, file_name, file_type, file_size, file_path, document_type, 
        tags, category, summary, keywords, view_count, download_count, status, 
        create_time, update_time, create_by, update_by, deleted
    </sql>

    <insert id="insert" parameterType="com.aicustomer.entity.KnowledgeDocument" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO knowledge_document (
            title, content, file_name, file_type, file_size, file_path, document_type,
            tags, category, summary, keywords, status, create_by, update_by
        ) VALUES (
            #{title}, #{content}, #{fileName}, #{fileType}, #{fileSize}, #{filePath}, #{documentType},
            #{tags}, #{category}, #{summary}, #{keywords}, #{status}, #{createBy}, #{updateBy}
        )
    </insert>

    <update id="update" parameterType="com.aicustomer.entity.KnowledgeDocument">
        UPDATE knowledge_document
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="fileName != null">file_name = #{fileName},</if>
            <if test="fileType != null">file_type = #{fileType},</if>
            <if test="fileSize != null">file_size = #{fileSize},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="documentType != null">document_type = #{documentType},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="category != null">category = #{category},</if>
            <if test="summary != null">summary = #{summary},</if>
            <if test="keywords != null">keywords = #{keywords},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE knowledge_document SET deleted = 1, update_time = NOW() WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM knowledge_document
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM knowledge_document
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        <if test="documentType != null and documentType != ''">
            AND document_type = #{documentType}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM knowledge_document
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        <if test="documentType != null and documentType != ''">
            AND document_type = #{documentType}
        </if>
    </select>

    <select id="searchFullText" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM knowledge_document
        WHERE deleted = 0 AND status = 1
        AND MATCH(content, title, summary) AGAINST(#{query} IN NATURAL LANGUAGE MODE)
        LIMIT #{limit}
    </select>

    <update id="incrementViewCount">
        UPDATE knowledge_document SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <update id="incrementDownloadCount">
        UPDATE knowledge_document SET download_count = download_count + 1 WHERE id = #{id}
    </update>

</mapper>
