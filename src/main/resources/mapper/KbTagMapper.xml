<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicustomer.mapper.KbTagMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.aicustomer.entity.KbTag">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="color" property="color"/>
        <result column="usage_count" property="usageCount"/>
        <result column="category" property="category"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 查询所有标签 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM kb_tag 
        WHERE deleted = 0
        ORDER BY usage_count DESC, create_time
    </select>

    <!-- 根据分类查询标签 -->
    <select id="selectByCategory" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT * FROM kb_tag 
        WHERE category = #{category} AND deleted = 0 AND is_active = 1
        ORDER BY usage_count DESC, create_time
    </select>

    <!-- 根据ID查询标签 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM kb_tag 
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 根据名称查询标签 -->
    <select id="selectByName" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT * FROM kb_tag 
        WHERE name = #{name} AND deleted = 0
    </select>

    <!-- 插入标签 -->
    <insert id="insert" parameterType="com.aicustomer.entity.KbTag" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_tag (
            name, description, color, usage_count, category, is_active,
            create_time, update_time, create_by, update_by, deleted, version
        ) VALUES (
            #{name}, #{description}, #{color}, #{usageCount}, #{category}, #{isActive},
            NOW(), NOW(), #{createBy}, #{updateBy}, 0, 1
        )
    </insert>

    <!-- 更新标签 -->
    <update id="update" parameterType="com.aicustomer.entity.KbTag">
        UPDATE kb_tag SET
            name = #{name},
            description = #{description},
            color = #{color},
            usage_count = #{usageCount},
            category = #{category},
            is_active = #{isActive},
            update_time = NOW(),
            update_by = #{updateBy},
            version = version + 1
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 删除标签 -->
    <update id="deleteById" parameterType="java.lang.Long">
        UPDATE kb_tag SET deleted = 1, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 更新使用次数 -->
    <update id="updateUsageCount">
        UPDATE kb_tag SET 
            usage_count = #{count},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 查询热门标签 -->
    <select id="selectHotTags" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT * FROM kb_tag 
        WHERE deleted = 0 AND is_active = 1
        ORDER BY usage_count DESC
        LIMIT #{limit}
    </select>

</mapper>
