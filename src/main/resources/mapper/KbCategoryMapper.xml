<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicustomer.mapper.KbCategoryMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.aicustomer.entity.KbCategory">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="parent_id" property="parentId"/>
        <result column="level" property="level"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="icon" property="icon"/>
        <result column="color" property="color"/>
        <result column="is_active" property="isActive"/>
        <result column="document_count" property="documentCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <!-- 查询所有分类 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM kb_category 
        WHERE deleted = 0 
        ORDER BY level, sort_order, create_time
    </select>

    <!-- 根据父分类ID查询子分类 -->
    <select id="selectByParentId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM kb_category 
        WHERE parent_id = #{parentId} AND deleted = 0
        ORDER BY sort_order, create_time
    </select>

    <!-- 根据ID查询分类 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM kb_category 
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 插入分类 -->
    <insert id="insert" parameterType="com.aicustomer.entity.KbCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_category (
            name, description, parent_id, level, sort_order, icon, color, 
            is_active, document_count, create_time, update_time,
            create_by, update_by, deleted, version
        ) VALUES (
            #{name}, #{description}, #{parentId}, #{level}, #{sortOrder}, #{icon}, #{color},
            #{isActive}, #{documentCount}, NOW(), NOW(),
            #{createBy}, #{updateBy}, 0, 1
        )
    </insert>

    <!-- 更新分类 -->
    <update id="update" parameterType="com.aicustomer.entity.KbCategory">
        UPDATE kb_category SET
            name = #{name},
            description = #{description},
            parent_id = #{parentId},
            level = #{level},
            sort_order = #{sortOrder},
            icon = #{icon},
            color = #{color},
            is_active = #{isActive},
            document_count = #{documentCount},
            update_time = NOW(),
            update_by = #{updateBy},
            version = version + 1
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 删除分类 -->
    <update id="deleteById" parameterType="java.lang.Long">
        UPDATE kb_category SET deleted = 1, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 更新文档数量 -->
    <update id="updateDocumentCount">
        UPDATE kb_category SET 
            document_count = #{count},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 查询启用的分类 -->
    <select id="selectActive" resultMap="BaseResultMap">
        SELECT * FROM kb_category 
        WHERE is_active = 1 AND deleted = 0
        ORDER BY level, sort_order, create_time
    </select>

</mapper>
