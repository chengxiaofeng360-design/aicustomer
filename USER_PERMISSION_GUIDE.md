# 用户权限管理系统 - 简化版

## 📋 概述

本系统提供了一个简化且易用的用户权限管理方案，让管理员可以快速创建用户并配置权限，无需复杂的角色和权限配置流程。

## 🎯 核心特性

### 1. **角色模板**
系统预设了5种常用角色模板，创建用户时只需选择对应模板即可：

| 角色模板 | 说明 | 默认权限 |
|---------|------|---------|
| **管理员** | 拥有所有权限 | 所有菜单 + 所有数据 + 可导出/删除 |
| **经理** | 大部分权限，不能管理用户 | 除系统管理外的所有菜单 + 所有数据 + 可导出/删除 |
| **普通员工** | 基础权限 | 客户管理、沟通记录、AI功能、团队协作、知识库 + 仅自己的数据 |
| **只读用户** | 只能查看 | 客户列表、沟通记录、知识库（只读）+ 仅自己的数据 |
| **自定义** | 手动选择菜单 | 需要手动勾选菜单权限 |

### 2. **菜单权限**
系统菜单采用树形结构，包含以下模块：

- 🏠 **首页** - 系统首页
- 👥 **客户管理** - 客户列表、新增客户、批量导入、数据导出
- 💬 **沟通记录** - 记录列表、新增记录
- 🤖 **AI功能** - AI对话、智能分析
- 👨‍👩‍👧‍👦 **团队协作** - 任务管理、进度汇报
- 📚 **知识库** - 文档管理、常见问题
- 📬 **消息中心** - 系统消息
- 📊 **数据报表** - 客户统计、业务分析
- ⚙️ **系统管理** - 用户管理、系统配置（仅管理员）

### 3. **数据权限**
精细化的数据访问控制：

#### 客户等级权限
- ✅ 所有等级 - 可以访问所有等级的客户
- 1️⃣ 普通客户 - 只能访问普通客户
- 2️⃣ VIP客户 - 只能访问VIP客户
- 3️⃣ 钻石客户 - 只能访问钻石客户

#### 数据范围
- **所有数据** - 可以查看和管理所有数据
- **本部门数据** - 只能查看和管理本部门的数据
- **仅自己的数据** - 只能查看和管理自己创建的数据

#### 操作权限
- 📤 **允许导出数据** - 可以导出Excel等格式的数据
- 🗑️ **允许删除数据** - 可以删除客户、沟通记录等数据

## 🚀 使用指南

### 创建新用户

1. **访问页面**
   ```
   http://localhost:8085/user-permission-simple.html
   ```

2. **点击"新建用户"按钮**

3. **填写基本信息**
   - 用户名（必填）
   - 密码（必填）
   - 真实姓名（必填）
   - 邮箱（选填）
   - 手机号（选填）
   - 状态（正常/禁用）

4. **选择角色模板**
   - 点击对应的角色卡片
   - 系统会自动配置该角色的默认权限
   - 如果选择"自定义"，需要手动勾选菜单权限

5. **配置数据权限**
   - 选择可访问的客户等级
   - 选择数据范围
   - 勾选是否允许导出/删除

6. **保存**
   - 点击"保存"按钮完成创建

### 编辑用户权限

1. 在用户列表中找到目标用户
2. 点击"编辑"按钮
3. 修改角色模板或数据权限
4. 保存更改

### 删除用户

1. 在用户列表中找到目标用户
2. 点击"删除"按钮
3. 确认删除操作

## 📡 API 接口

### 获取菜单列表
```
GET /api/user-permission/menus
```

### 获取角色模板
```
GET /api/user-permission/role-templates
```

### 获取模板的默认菜单
```
GET /api/user-permission/role-template/{template}/menus
```

### 创建用户
```
POST /api/user-permission/create
Content-Type: application/json

{
  "username": "zhangsan",
  "password": "123456",
  "realName": "张三",
  "email": "zhangsan@example.com",
  "phone": "13800138000",
  "status": 1,
  "roleTemplate": "employee",
  "menuPermissions": ["home", "customer", "customer-list"],
  "dataPermission": {
    "customerLevels": [0],
    "dataScope": "self",
    "canExport": false,
    "canDelete": false
  }
}
```

### 更新用户权限
```
PUT /api/user-permission/update
Content-Type: application/json

{
  "id": 1,
  "realName": "张三",
  "roleTemplate": "manager",
  ...
}
```

### 获取用户权限
```
GET /api/user-permission/{userId}
```

### 获取客户等级选项
```
GET /api/user-permission/customer-levels
```

### 获取数据范围选项
```
GET /api/user-permission/data-scopes
```

## 🎨 界面预览

### 用户列表
- 显示所有用户的基本信息和角色
- 支持快速编辑和删除操作

### 创建/编辑用户
- 分步骤的表单设计
- 角色模板卡片式选择
- 树形菜单权限配置
- 直观的数据权限设置

## 💡 最佳实践

1. **新员工入职**
   - 选择"普通员工"模板
   - 数据范围设置为"仅自己的数据"
   - 不勾选导出和删除权限

2. **部门经理**
   - 选择"经理"模板
   - 数据范围设置为"本部门数据"
   - 勾选导出权限，谨慎开启删除权限

3. **系统管理员**
   - 选择"管理员"模板
   - 拥有所有权限

4. **临时查看人员**
   - 选择"只读用户"模板
   - 只能查看数据，不能修改

## 🔒 安全建议

1. **密码策略**
   - 建议使用强密码（至少8位，包含字母、数字、特殊字符）
   - 定期更换密码

2. **权限最小化原则**
   - 只授予用户必要的权限
   - 定期审查用户权限

3. **敏感操作**
   - 导出和删除权限应谨慎授予
   - 重要操作应记录日志

## 📝 后续优化计划

1. ✅ 简化权限配置流程
2. ✅ 预设角色模板
3. ✅ 可视化菜单权限选择
4. ✅ 客户等级权限控制
5. 🔄 数据权限实际应用到查询过滤
6. 🔄 操作日志记录
7. 🔄 权限变更通知
8. 🔄 批量用户导入

## 🆘 常见问题

**Q: 如何快速创建多个相同权限的用户？**
A: 先创建一个用户并配置好权限，后续用户选择相同的角色模板即可。

**Q: 用户忘记密码怎么办？**
A: 管理员可以在编辑用户时重新设置密码。

**Q: 如何临时禁用某个用户？**
A: 编辑用户，将状态改为"禁用"即可。

**Q: 自定义角色和预设角色有什么区别？**
A: 预设角色有固定的菜单权限配置，自定义角色需要手动选择每个菜单。

## 📞 技术支持

如有问题，请联系系统管理员或查看系统文档。
