# ✅ 部署前检查清单 - 百度云Linux服务器

## 🎯 打包信息
- **JAR文件**: `ai-customer-management-1.0.0.jar`
- **构建时间**: 2026-01-21 00:15:30
- **构建状态**: ✅ BUILD SUCCESS
- **构建耗时**: 9.941秒

## ⚠️ 已修复的关键问题

### 1. ✅ SQL自动执行问题（严重）
- **问题**: `spring.sql.init.mode: always` 会在每次启动时执行SQL
- **风险**: 可能重置数据库，导致数据丢失或密码重置
- **修复**: 改为 `mode: never`
- **状态**: ✅ 已修复

### 2. ✅ 删除自动初始化用户方法
- **问题**: `UserServiceImpl.initAdmin()` 会自动创建默认密码123456的账号
- **风险**: 安全隐患
- **修复**: 已完全删除该方法
- **状态**: ✅ 已修复

### 3. ✅ 向量搜索localhost配置
- **问题**: Elasticsearch和embedding服务配置为localhost
- **修复**: 已注释掉，需要时手动开启
- **状态**: ✅ 已修复

### 4. ✅ 删除无用配置
- **删除**: `application-secret.yml`、`application-prod.yml`（多余）
- **删除**: Jasypt加密配置（未使用）
- **状态**: ✅ 配置已简化

## 📋 部署前核对清单

### 配置文件检查
- [x] `application.yml` - 所有路径已配置为生产环境路径
- [x] 日志路径: `/data/logs/app/ai-customer.log`
- [x] 上传路径: `/data/files/upload/`
- [x] SQL自动执行: `mode: never` ✅
- [x] 数据库连接: 使用正确的数据库地址
- [x] 数据库密码: 已配置（`Bjzk60166200`）

### 安全检查
- [x] 没有自动创建默认密码账号
- [x] SQL不会自动执行
- [x] 敏感配置文件已排除在Git外

### 部署脚本检查
- [x] `deploy/bin/start.sh` - 启动脚本 ✅
- [x] `deploy/bin/stop.sh` - 停止脚本 ✅
- [x] `deploy/bin/restart.sh` - 重启脚本 ✅
- [x] `deploy/bin/status.sh` - 状态查询脚本 ✅

### JAR包检查
- [x] JAR包已生成: `target/ai-customer-management-1.0.0.jar`
- [x] 文件大小正常
- [x] 包含所有依赖

## 🚀 部署步骤

### 步骤1: 上传文件到百度云服务器
```bash
# 上传JAR包
scp target/ai-customer-management-1.0.0.jar root@百度云IP:/data/app/jars/

# 上传脚本
scp deploy/bin/*.sh root@百度云IP:/data/app/bin/

# 上传SQL脚本（首次部署需要）
scp src/main/resources/sql/init.sql root@百度云IP:/tmp/
scp src/main/resources/sql/create_initial_admin.sql root@百度云IP:/tmp/
```

### 步骤2: SSH登录服务器
```bash
ssh root@百度云IP
```

### 步骤3: 创建目录结构
```bash
sudo mkdir -p /data/app/jars
sudo mkdir -p /data/app/bin
sudo mkdir -p /data/files/upload
sudo mkdir -p /data/logs/app
sudo mkdir -p /data/tmp
sudo mkdir -p /data/backup

# 设置权限
chmod +x /data/app/bin/*.sh
```

### 步骤4: 初始化数据库（首次部署）
```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE IF NOT EXISTS zqgl CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出MySQL
exit;

# 执行SQL脚本
mysql -u root -p zqgl < /tmp/init.sql

# 创建管理员账号
mysql -u root -p zqgl < /tmp/create_initial_admin.sql
```

### 步骤5: 启动应用
```bash
/data/app/bin/start.sh
```

### 步骤6: 验证部署
```bash
# 查看状态
/data/app/bin/status.sh

# 查看日志
tail -f /data/logs/app/ai-customer.log

# 测试接口
curl http://localhost:8085/actuator/health
```

## ⚙️ 重要配置说明

### 数据库配置
- **地址**: 腾讯云 CynosDB
- **端口**: 26713
- **数据库名**: zqgl
- **用户名**: root
- **密码**: 已在 `application.yml` 中配置

### 应用配置
- **端口**: 8085
- **JVM内存**: 512MB-2GB（可在start.sh中调整）
- **日志策略**: 单文件最大100MB，保留30天，总容量3GB

## 🔧 后续配置（可选）

### 1. 配置防火墙
```bash
# 开放8085端口
firewall-cmd --permanent --add-port=8085/tcp
firewall-cmd --reload

# 或使用 ufw (Ubuntu)
ufw allow 8085/tcp
```

### 2. 配置Nginx反向代理（推荐）
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://localhost:8085;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 3. 设置开机自启
```bash
# 创建systemd服务
sudo vi /etc/systemd/system/ai-customer.service
# 复制 deploy/部署说明.md 中的服务配置

# 启用服务
sudo systemctl daemon-reload
sudo systemctl enable ai-customer
sudo systemctl start ai-customer
```

### 4. 配置定时备份
```bash
# 添加到crontab
crontab -e
# 添加：每天凌晨2点备份
0 2 * * * mysqldump -u root -p'密码' zqgl > /data/backup/zqgl-$(date +\%Y\%m\%d).sql
```

## 🎯 管理员账号（首次登录）

- **用户名**: admin
- **密码**: Admin@2024（或您自定义的密码，见create_initial_admin.sql）
- **⚠️ 重要**: 首次登录后请立即修改密码！

## 📞 故障排查

### 应用无法启动
1. 检查Java版本: `java -version` （需要Java 17+）
2. 检查端口占用: `netstat -tlnp | grep 8085`
3. 查看日志: `tail -100 /data/logs/app/console.log`

### 数据库连接失败
1. 检查数据库服务: `systemctl status mysql`
2. 测试连接: `mysql -u root -p zqgl`
3. 检查防火墙规则

### 文件上传失败
1. 检查目录权限: `ls -ld /data/files/upload/`
2. 创建目录: `mkdir -p /data/files/upload/ && chmod 755 /data/files/upload/`

## ✅ 部署完成确认

部署完成后，请确认以下项目：

- [ ] 应用成功启动（`/data/app/bin/status.sh`显示运行中）
- [ ] 可以访问 http://服务器IP:8085
- [ ] 可以使用admin账号登录
- [ ] 数据库连接正常
- [ ] 日志正常输出到 `/data/logs/app/ai-customer.log`
- [ ] 文件上传功能正常

---

**部署时间**: 2026-01-21
**版本**: 1.0.0
**打包状态**: ✅ SUCCESS
